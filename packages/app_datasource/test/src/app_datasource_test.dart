// ignore_for_file: prefer_const_constructors
import 'package:app_datasource/app_datasource.dart';
import 'package:test/test.dart';

void main() {
  group('AppDatasource', () {
    test('can be instantiated', () {
      expect(AppDatasource(), isNotNull);
    });
  });
}
